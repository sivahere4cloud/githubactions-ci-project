dfname: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted
    steps:
      # Stage 1: Checkout Code
      - name: Checkout repository
        uses: actions/checkout@v4
